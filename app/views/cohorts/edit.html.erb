<%= form_for(@cohort) do |f| %>
<%= render 'shared/errors', object: @cohort %>

<%= f.label :start_date %>
<%= f.date_field :start_date %>

<%= f.label :season %>
<%= f.text_field :season %>

<%= f.label :year %>
<%= f.text_field :year %>

<%= f.submit "Update Cohort" %>

<% end %>
